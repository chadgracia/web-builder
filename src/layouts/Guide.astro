---
import Header from '../components/Header.astro';

interface Props {
  title: string;
  description?: string;
  date?: string;
  slug: string;
  canonical?: string;
}

const { title, description, date, slug, canonical } = Astro.props;
const tradesUrl = `https://trades.graciagroup.com/?src=${slug}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    {canonical && <link rel="canonical" href={canonical} />}
    <link rel="stylesheet" href="https://s3.us-east-1.amazonaws.com/main.css/master.css" />
  </head>
  <body>
    <Header tradesUrl={tradesUrl} />

    <main class="guide">
      <article class="report">
        <div class="article-head">
          <h1>{title}</h1>
          {date && <p class="article-date">{date}</p>}
        </div>
        <slot />
      </article>
      <aside class="deal-panel" aria-hidden="true">
        <!-- Reserved for live deal data -->
      </aside>
    </main>

    <footer class="site-footer">
      <a class="btn" href={tradesUrl}>Work with Gracia Group</a>
    </footer>
  </body>
</html>

<style>
  .article-head h1 { margin-bottom: 4px; }
  .article-date { color: var(--text-muted, #6b7280); font-size: 14px; margin: 0 0 1.5rem; }

  .guide {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 280px;
    gap: 2rem;
    max-width: 64rem;
    margin: 0 auto;
    padding: 2rem;
  }

  .deal-panel {
    border: 1px dashed var(--border, #c4c4c4);
    border-radius: 8px;
    background: #fafafa;
    min-height: 200px;
  }

  .site-footer {
    padding: 2rem;
    border-top: 1px solid var(--border, #e5e5e5);
    text-align: center;
  }

  @media (max-width: 720px) {
    .guide {
      grid-template-columns: 1fr;
    }
  }
</style>
